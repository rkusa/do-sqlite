{
  "name": "do-sqlite-asyncify",
  "version": "1.0.0",
  "main": "dist/worker.js",
  "scripts": {
    "build": "esbuild --format=esm --platform=neutral --loader:.wasm=file --loader:.js=file --outdir=./dist --bundle src/worker.ts",
    "format": "prettier --write  '*.{json,js}' 'src/**/*.{js,ts}' 'test/**/*.{js,ts}'",
    "lint": "prettier --check '*.{json,js}' 'src/**/*.{js,ts}' 'test/**/*.{js,ts}'"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^3.2.0",
    "@tsconfig/recommended": "^1.0.1",
    "esbuild": "^0.14.11",
    "typescript": "^4.3.2"
  },
  "dependencies": {
    "@cloudflare/wrangler": "^1.19.5",
    "asyncify-wasm": "^1.2.1"
  }
}
