{
  "name": "do-sqlite-asyncify",
  "version": "1.0.0",
  "main": "dist/worker.js",
  "scripts": {
    "build": "./build.sh",
    "prepare": "npm run build",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^3.14.0",
    "@tsconfig/recommended": "^1.0.1",
    "esbuild": "^0.14.48",
    "typescript": "^4.7.4"
  },
  "dependencies": {
    "@cloudflare/wrangler": "^1.19.12",
    "@rkusa/wasm-sqlite": "^0.1.0"
  }
}
